import { StyleSheet } from 'react-native';

import container from '../../assets/styles/MainContainerStyles';

const styles = StyleSheet.create({
    container,
    divider: {
        flex: 1,
    },
    input: {
        marginTop: 12,
        width: '90%',
    },
    button: {
        marginTop: 12,
        width: '90%',
        padding: 8,
    },
});

export default styles;
