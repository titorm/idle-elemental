import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    item: {
        marginBottom: 16,
    },
    image: {
        height: 48,
        width: 48,
    },
});

export default styles;
